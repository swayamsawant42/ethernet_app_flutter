name: itechseed_crm_ethernetxpress
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter_secure_storage: ^9.0.0
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  share_plus: ^10.1.3

  google_maps_flutter: ^2.9.0
  permission_handler: ^11.3.1
  geolocator: ^13.0.2
  image_picker: ^1.1.2
  flutter_image_compress: ^2.3.0
  dio: ^5.7.0
  shared_preferences: ^2.3.3
  intl: ^0.19.0
  geocoding: ^2.1.3
  sqflite: ^2.3.0
  path: ^1.9.0
  connectivity_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # --- ADD THESE LINES ---
  # Make sure the indentation is correct (two spaces)
  assets:
    - assets/EXPL-Logo.png